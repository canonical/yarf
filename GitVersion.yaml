workflow: TrunkBased/preview1
commit-message-incrementing: Enabled
major-version-bump-message: '^(?i:(\[Breaking\]|Breaking:))'
minor-version-bump-message: '^(?i:(feat:|\[New\]|Add:))'
# patch-version-bump-message: '^(?i:(\[BugFix\]|Fix:|\[Infra\]))'
branches:
  main:
    regex: ^main$         # Place your branch name here to emulate main branch behavior.
    increment: Patch
    is-main-branch: true

# PEP440 is not compatible with Semantic Versioning 🥹, therefore for non-main branches version will be set from InformationalVersion
assembly-informational-format: '{Major}.{Minor}.{Patch}+{BranchName}.{ShortSha}'
