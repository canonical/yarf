#!/bin/sh
set -e

SRC_DIR="$SNAP/lib/python3.12/site-packages/yarf_plugin_example"
DEST_DIR="$SNAP_COMMON/platform_plugins/yarf_plugin_example"

mkdir -p "$DEST_DIR"
cp -r "$SRC_DIR"/* "$DEST_DIR"

echo "Plugin files copied to $DEST_DIR"
